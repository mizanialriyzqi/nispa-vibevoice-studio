/**
 * Shared Voice type. Use this instead of local redefinitions.
 * Re-exported from GlobalContext for backward compatibility.
 */
export type { Voice } from '../context/GlobalContext';
export type { Model } from '../context/GlobalContext';
