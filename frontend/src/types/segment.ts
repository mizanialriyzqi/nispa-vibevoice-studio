/**
 * Shared SubtitleSegment type with audio and metadata fields.
 * Re-exported from SubtitleContext for backward compatibility.
 */
export type { SubtitleSegment } from '../features/subtitle/context/SubtitleContext';
